import 'package:flutter/material.dart';

class PaymentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mobile Money Payment'),
      ),
      body: Center(
        child: Text('This is the mobile money payment page.'),
      ),
    );
  }
}
